exports.config = {
    host: '127.0.0.1',
    port: 3000,
    // uncomment the following to enable caching of all templates
    globalTemplateCaching: false,
    // uncomment to enable caching for this template
    singleTemplateCache: false,
    viewDirectory: `views`,
    debug: false
}